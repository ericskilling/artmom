{
  "name": "rachelle-portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync:images": "rsync -ar --delete src/assets/images/ public/assets/images/",
    "dev": "npm run sync:images && astro dev --port 3000",
    "build": "npm run sync:images && astro build",
    "build:prod": "npm run sync:images && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.3",
    "astro-seo": "^1.1.0",
    "marked": "^18.0.3",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
